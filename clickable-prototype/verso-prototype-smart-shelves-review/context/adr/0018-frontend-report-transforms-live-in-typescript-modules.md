# Frontend Report Transforms Live in TypeScript Modules

Frontend-owned report transforms live in plain TypeScript modules imported by React components rather than inline component code. This keeps genre aggregation, cadence bucketing, cost calculations, author concentration, narrator rankings, and chart shaping testable without rendering the UI.