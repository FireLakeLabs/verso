# ASIN Identifies Audible Items

Verso uses ASIN as the stable identity for an Audible Item. This keeps tags, journal entries, snapshots, health-check acknowledgements, collection membership, export selections, and privacy choices attached to the exact Audible library entry while allowing duplicate detection and work-level views to exist as separate grouping behavior rather than automatic merges.