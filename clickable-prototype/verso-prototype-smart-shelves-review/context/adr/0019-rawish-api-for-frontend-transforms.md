# Raw-ish API for Frontend Transforms

Verso's backend exposes raw-ish library data for frontend TypeScript report modules to transform, rather than pre-shaped endpoint responses per visual report. The API still owns commands and focused queries for refresh, export generation, tag mutation, Smart Shelf persistence, settings, snapshots, Health Findings, and Finding Dispositions.