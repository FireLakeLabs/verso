# Live AudibleApi Import Primary

Verso uses live AudibleApi import as the primary ingestion source for Solid v1. File-based imports from tools such as Libation may be added later as fallback adapters, but refresh, Current Audible Facts, completion snapshots, returnability, price observations, and plan availability are designed around direct AudibleApi data first.