# ASP.NET Core API Backend

Verso uses an ASP.NET Core API backend for Solid v1. The backend owns AudibleApi integration, SQLite access, snapshots, and export generation, while the Vite React frontend consumes API endpoints for local interactive workflows.