# Separate Local Frontend and Backend Servers

Verso may run Solid v1 as separate local frontend and backend servers rather than one integrated production process. This favors a clean frontend/backend boundary and straightforward development over single-command packaging, which is acceptable because the product is a local on-demand tool for one Library Owner.