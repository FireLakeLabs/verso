# Recharts Default Visualization Library

Verso uses Recharts as the default visualization library for Solid v1 charts and adds specialized visualization libraries only where Recharts is a poor fit, such as treemaps, heatmaps, or wordclouds. This keeps the report surface visually consistent without forcing every report through one toolkit when the fit is weak.