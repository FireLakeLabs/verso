# Root Dev Orchestration

Verso provides root-level development orchestration to start the local backend and frontend servers together for convenience. Each app remains independently runnable with its normal backend or frontend commands so debugging and focused development do not depend on the root orchestration layer.